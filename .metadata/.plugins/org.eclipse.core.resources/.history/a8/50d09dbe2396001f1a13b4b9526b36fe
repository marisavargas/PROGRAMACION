package ejercicio;
import java.util.Scanner;
public class ejercici8repaso {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
		Scanner scanner = new Scanner(System.in);
		String numeroA1= scanner.next();//ojo hay que declararlo arriba del do para que sirvan las variahles para todo
		String numeroB1= scanner.next();// en este caso hay que poner otra variable por que no es un numero
		do {
			System.out.println("Dame un numero:");

			System.out.println("Dame un numero: ");

			                                                               
		} while( !numeroA1.equals(numeroB1));// estamos usando .equals()
		System.out.println("Saludo");
		scanner.close();
	}  
}
